namespace Edgar.Dossier.Core
{
    public enum ClueCategory
    {
        Evidence,       // Physical objects (knife, note, photo)
        Observation,    // Scene details (open window, moved bookshelf)
        Statement,      // NPC dialogue (butler's alibi)
        Thought,        // Player deductions (the butler lied!)
    }
}